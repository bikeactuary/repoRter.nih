
hexSticker::sticker(subplot = "man/figures/heart.png",
                    package = "repoRter.nih",
                    filename = "man/figures/hexlogo.png",
                    s_y = 1,                          # Position of the sub plot (y)
                    s_x = 1,                       # Position of the sub plot (x)
                    s_width = .6,                   # Width of the sub plot
                    # s_height=0.01,                    # Height of the sub plot
                    # Font
                    p_size = 28,                       # Font size of the text
                    p_y = .6,                        # Position of the font (y)
                    p_x=1.45,                         # Position of the font (x)
                    # p_family = "incon",               # Defines font
                    # Spotlight
                    spotlight = TRUE,                 # Enables spotlight
                    l_y=1.08,                          # Position of spotlight (y)
                    l_x=.71,                          # Position of spotlight (x)
                    # Sticker colors
                    h_fill = "#6f1200",               # Color for background
                    h_color = "#551900",              # Color for border
                    # Resolution
                    dpi=1200  )
